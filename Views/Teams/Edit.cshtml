@model LeagueOrganizer.Models.Team

@{
    ViewData["Title"] = "Team - Edit";
}
<h2>Edit</h2>
<hr />
<h4>Edit this item: @Html.DisplayFor(model => model.Name)</h4>
@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.TeamId)

    @Html.LabelFor(model => model.Name)
    @Html.EditorFor(model => model.Name)

    @Html.LabelFor(model => model.DivId)
    @Html.DropDownList("DivId")

    @Html.LabelFor(model => model.CaptId)
    @Html.DropDownList("CaptId")

    <input type="submit" value="Save" class="btn btn-default" />
}
@Html.ActionLink("Back to List", "Index")